bunx lint-staged
# Real binary — the npm package named "gitleaks" is an unrelated stub with no bin.
if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks not on PATH. Install v8.30.1 from https://github.com/gitleaks/gitleaks/releases"
  exit 1
fi
gitleaks protect --staged --no-banner --redact
