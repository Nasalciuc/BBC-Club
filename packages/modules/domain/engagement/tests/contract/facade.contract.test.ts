import { describe, it } from "bun:test";
describe("@bbc/engagement facade", () => {
  it.todo("TODO: contract test for engagement — write it before merging (a todo keeps validate red)", () => {});
});
